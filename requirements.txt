# Vercel Serverless — lightweight deps (under 250MB)
# Uses Gemini REST API via httpx instead of heavy google-generativeai SDK
# Heavy libs (PyMuPDF, Pillow, trafilatura) excluded; features gracefully degrade

fastapi==0.115.0
httpx==0.28.0
python-multipart==0.0.18
python-dotenv==1.1.0
beautifulsoup4==4.12.3
langdetect==1.0.9
